// Placeholder webview entry. The real Aurora Preview webview UI (Svelte)
// is added in a later task, which replaces this file.
const app = document.getElementById('app');
if (app) {
  app.textContent = 'Aurora Preview: coming online…';
}

export {};
